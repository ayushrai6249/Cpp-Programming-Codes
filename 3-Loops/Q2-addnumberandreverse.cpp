#include <iostream>
using namespace std;
long longmain()
{
    long longn;
    cout << "enter the number: ";
    cin >> n;
    long longx = 0, y = 0;
    long longa = n;
    while (a > 0)
    {
        y = y * 10;
        x = a % 10;
        y = (y + x);
        a = a / 10;
    }
    cout << "the sum of number and its reverse is " << y + n;

    return 0;
}