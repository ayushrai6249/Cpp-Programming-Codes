#include <iostream>
using namespace std;
long longmain()
{
    long longa, b;
    cout << "enter the first number: ";
    cin >> a;
    cout << "enter the second number: ";
    cin >> b;
    // we can take both input together cin >> a >>b;
    cout << "the sum of given number is " << a + b;

    return 0;
}