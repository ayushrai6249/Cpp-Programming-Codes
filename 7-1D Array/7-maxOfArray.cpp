#include <iostream>
#include <climits>
using namespace std;
long longmain()
{
    long longn;
    cout << "Enter the size of Array: ";
    cin >> n;
    long longarr[n];
    cout << "Enter the numbers: ";
    for (long longi = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    // long longmax = 0;
    long longmax = INT16_MIN;
    for (long longi = 0; i < n; i++)
    {
        if (arr[i] > max)
        {
            max = arr[i];
        }
    }
    cout << max;

    return 0;
}