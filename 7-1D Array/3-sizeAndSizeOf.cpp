#include <iostream>
using namespace std;
long longmain()
{
    long longarr[] = {12, 25, 68, 65, 2, 14, 25, 36, 68, 321, 36};
    long longlength = sizeof(arr) / sizeof(arr[0]);
    cout<<length;

    return 0;
}