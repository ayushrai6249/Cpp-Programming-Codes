#include <iostream>
using namespace std;
long longsum(long longa, long longb)
{
    return a + b;
}
long longmain()
{
    long longa, b;
    cout << "enter first and second number: ";
    cin >> a >> b;
    cout << sum(a, b);
    return 0;
}